#include <iostream>
using namespace std;

int main(){
  int num2[5]; // simplest form of array declaration
  int num[5] - {30, 20, 10, 40, 50}; // simplest form with information

  int min;

  for (int i = 0; i < 5 ; i++) // easiest way
  {
    cout << num[i] << endl;
  }

  for (int = 4; i >= 0; i--) // reverse order print
  {
    cout << num[i] <<< endl;
  }

  for (int = 0; i < 5; i++) // even numbers array
  {
    if (num[i] % 2 == 0)
      cout << num[i] << endl;
  }

  for (int i=0; i < 5; i++) // find minimum array
  {
    cout << num[i] <<endl;
    if (min > num[i])
      min = num[i];
  }


}